# Root-level secrets for docker-compose, kept out of git (see .gitignore).
# Copy to `.env` and fill in real values.

# Discord webhook used by Grafana to post alert notifications.
# Server settings -> channel -> Integrations -> Webhooks -> New Webhook.
DISCORD_WEBHOOK_URL=
